---
title: '离线底图'
description: '用磁盘上现成的瓦片包作为底图，带层级边界和按视图关闭的开关，完全不联网。'
---

# 离线底图

<!-- nav:start -->

[English](../en/offline-basemap.md) · **简体中文** · [指南目录](README.md)

<!-- nav:end -->

把所有地图指向磁盘上已有的一整套瓦片，笔记底下那层地图就不再需要网络了。其他部分本
来就是离线的 —— 笔记、轨迹、照片和缩略图都是仓库里的文件 —— 所以这是最后一块。

Advanced Maps **不下载瓦片**。批量抓取某家服务商的瓦片是人家授权的事，不是这个插件
替你决定的事。这里做的只是指向你已经有的那套瓦片，然后读它。

## 什么算一套瓦片

一个按 `z/x/y` 排布的图片文件夹，也就是所有滑动地图的寻址方式：

```text
tiles/
  6/
    52/
      25.png
      26.png
  7/
    104/
      50.png
```

能解压成这个形状的都可以。**不行**的是单文件的 `.mbtiles` 或 `.pmtiles`：读它需要往
地图库里挂一个协议处理器，而这个插件并不自带那个库。先解包成目录树，解出来的就是一
套瓦片。

把这套瓦片放在**仓库外面**。一份区域瓦片动辄十万个文件，而仓库里的东西 Obsidian 都
会索引。机器上任何路径都行。

## 指向它

**设置 → 第三方插件 → Advanced Maps → 离线底图。**

| 行           | 填什么                                                      |
| ------------ | ----------------------------------------------------------- |
| 瓦片路径     | `/home/you/tiles/{z}/{x}/{y}.png`，也就是你的文件的寻址形状 |
| 瓦片最浅层级 | 你的 `z` 目录里编号最小的那个                               |
| 瓦片最深层级 | 编号最大的那个                                              |

路径可以是绝对路径，也可以相对于仓库。`{z}`、`{x}`、`{y}` 会逐块替换；TMS 行序排布
的瓦片用 `{-y}` 也认。路径留空，各地图就保持它原有的底图。

这里填的是文件系统路径，不是 URL。插件会在建地图的时候把它变成 URL —— 因为那个 URL
需要的前缀每次启动 Obsidian 都会重新生成，手写的 URL 能用到下次重启为止。

### 那两个层级

它们就是你这套瓦片两端的文件夹名字，各挡住一种不同的问题：

- **最深层级**约束瓦片本身。再往里放大时，地图会把已有的最深瓦片放大接着画，而不是
  去要根本不存在的文件。填小了会损失本来有的清晰度；填大了地图会为超出范围的每一块
  瓦片默默发一次读取失败。
- **最浅层级**约束镜头。往外缩到这里就停住，而不是变成空白 —— 因为你最浅那层之上没
  有东西可以放大了。

如果这套瓦片覆盖 `z0`–`z14`，就填 0 和 14。

## 按地图区分

地图视图选项的**背景**一节里多了一项**离线底图**，默认是用。所以配好一套瓦片就会到
达所有地图；某个视图想保留自己的底图（卫星图、国内底图，或者那个视图本来配的任何东
西），把它设成"不用"。

视图自己的**地图瓦片**设置从来不会被改写。离线底图是在建地图的那一刻替换进去的，所
以在某个视图上关掉它，那个视图原本配的东西会原样回来，没有什么需要撤销。

内联 `![[route.gpx]]` 地图没有自己的视图选项，跟随插件设置。

## 坐标系

本地路径里没有服务商域名，所以**自动**会把这套瓦片当成 WGS-84 —— 对绝大多数基于
OpenStreetMap 的瓦片来说这是对的。如果你这套是从国内服务商那里解包来的，它是
GCJ-02，而自动模式看不出来：在设置 →**坐标系**里说明，或者用视图自己的**瓦片坐标系**
选项。见[坐标与地图服务](coordinates-and-services.md)。

## 什么都没画出来的时候

地图变成一片背景色，图钉和轨迹还在。按顺序检查：

1. **路径。** 要填到瓦片本身，含 `{z}/{x}/{y}` 和扩展名，不是它们上面那层文件夹。把
   数字代进去 `ls` 一下；那个文件不在，瓦片就不在。
2. **扩展名。** `.png`、`.jpg`、`.webp` 都行，但必须是你的文件实际用的那个。
3. **两个层级。** 最浅层级比地图当前位置还深，镜头会被顶住；最深层级填成 0，整个世
   界就只剩一块瓦片。
4. **这个视图。** 它的**离线底图**选项可能被设成了"不用"。

以上都在桌面端实测。移动端读本地文件走的是另一条路，未做验证；即使不生效也不会坏什
么，地图就是继续用网络底图。

## 这个功能会动什么

什么都不动。这套瓦片只被打开读取，不会被写入、移动或删除，也没有任何一步会去服务商
那里抓瓦片。地图在画这套瓦片的时候，完全不向网络发瓦片请求 —— 什么东西会离开仓库，
见[参考与隐私](reference-and-privacy.md)。
