## 1. The contract

- [x] 1.1 Add the mobile-verification requirement to `maintainer-workflow`, naming what emulation covers and what only a device settles
- [x] 1.2 Restate the guide requirement once, discarding the duplicated copy of its body and four scenarios

## 2. The procedure

- [x] 2.1 Add a **Testing on mobile** section to `CONTRIBUTING.md`: the emulation command, and what it cannot answer
- [x] 2.2 Name the device path — getting the three built files into a vault on a phone, and reading that vault's console over USB debugging
- [x] 2.3 List the claims that need a device, so the list is a checklist rather than a caution

## 3. Proof

- [x] 3.1 `npm run check` passes: no executable change, so this is formatting, links, and the documentation reference check
