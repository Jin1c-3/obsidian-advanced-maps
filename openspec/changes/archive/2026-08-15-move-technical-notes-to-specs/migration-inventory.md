## Baseline validation

Captured on 2026-08-15 from `docs/move-technical-notes-to-specs`, before editing contributor documentation or TypeScript comments.

- `npm run check`: failed at `prettier --check .` before lint, typecheck, tests, manifest validation, build, or smoke. Prettier reported 29 pre-existing unformatted files, all in the untracked OpenSpec/Codex/Claude scaffolding: `.agents/skills/openspec-*`, `.claude/commands/opsx/*`, `.claude/skills/openspec-*`, `openspec/changes/move-technical-notes-to-specs/design.md`, and its ten delta specs.
- `node esbuild.config.mjs production && npm run smoke`: passed. The production bundle was written to `main.js`; the smoke loader confirmed that it loads, exports `Pt`, and requires only `obsidian`.

## `CLAUDE.md` heading inventory

The destination column names the canonical capability or documentation role. `design/history` means the still-useful decision is retained by this change's design rather than copied into a behavior spec; `obsolete` means it is deliberately discarded.

| Current heading                                                       | Classification and canonical destination                                                                                                                             |
| --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLAUDE.md`                                                           | contributor/README → concise entry point; ownership rules in `maintainer-workflow`                                                                                   |
| What this plugin is                                                   | spec → `native-map-integration`                                                                                                                                      |
| Commands                                                              | contributor/README → `CLAUDE.md` and `CONTRIBUTING.md`                                                                                                               |
| Layout                                                                | contributor/README → compact `CLAUDE.md` source map                                                                                                                  |
| How the patch works                                                   | spec → `native-map-integration`; symbol-level rationale → design/keep-inline                                                                                         |
| House rules for the patching code                                     | contributor/README → `CLAUDE.md`; provenance detail → keep-inline                                                                                                    |
| How tracks find their way onto the map                                | spec → `track-map-rendering`                                                                                                                                         |
| All three ways of pointing at a file count, and `!` is the difference | spec → `track-map-rendering` and `inline-track-maps`; issue transcript → design/history                                                                              |
| Coordinates from a photo's EXIF                                       | spec → `photo-map-rendering`                                                                                                                                         |
| The head-read                                                         | spec → `photo-map-rendering`; measurements and platform gaps → design/history/test                                                                                   |
| The datum                                                             | spec → `photo-map-rendering` and `coordinate-datum`; live-photo transcript → design/history/test                                                                     |
| The thumbnail is the whole album                                      | spec → `photo-map-rendering`; measured byte counts → design/history                                                                                                  |
| Zoom-dependent density is symbol collision, not clustering            | spec → `photo-map-rendering`; rejected clustering alternative → design/history                                                                                       |
| Hovering gives the note, clicking gives the photo                     | spec → `photo-map-rendering` and `note-map-navigation`                                                                                                               |
| An inline map draws its host note's photos                            | spec → `inline-track-maps` and `photo-map-rendering`                                                                                                                 |
| Traps, same register as "Non-obvious things to leave alone"           | spec scenarios → `photo-map-rendering`; layer/cache/decode rationale → design/history/keep-inline                                                                    |
| What was not measured (photos)                                        | design/history and test gaps                                                                                                                                         |
| Pins that share a spot                                                | spec → `pin-spreading`                                                                                                                                               |
| `icon-offset`, not a moved coordinate                                 | spec → `pin-spreading`; rejected alternatives and live probes → design/history                                                                                       |
| Integer slots, not an array property                                  | design/history; expression contract → keep-inline/test                                                                                                               |
| Which pins count as sharing a spot                                    | spec → `pin-spreading`; tolerances → test                                                                                                                            |
| The seams, and what has to happen at each                             | spec → `pin-spreading` and `native-map-integration`; symbol-level ordering → keep-inline                                                                             |
| A trap that cost a debugging cycle                                    | design/history; stale test precondition → obsolete                                                                                                                   |
| What was not measured (pin spreading)                                 | design/history and test gaps                                                                                                                                         |
| Coordinate systems (GCJ-02 / BD-09)                                   | spec → `coordinate-datum`                                                                                                                                            |
| Every place a coordinate crosses the line                             | spec → `coordinate-datum`; exact seams → design/keep-inline                                                                                                          |
| "Open in map", and following the active note                          | spec → `note-map-navigation`                                                                                                                                         |
| `focus()`, and the three things that take the camera back             | spec → `note-map-navigation`; timing transcript → design/history/test                                                                                                |
| Following                                                             | spec → `note-map-navigation`                                                                                                                                         |
| Sidebar-only was the wrong rule, and a button is the right one        | spec → `note-map-navigation`; rejected location rule → design/history                                                                                                |
| The two things that made a following map unusable beside a note       | spec → `note-map-navigation`; debugging transcript → design/history/test                                                                                             |
| A map of the notes around a note                                      | spec → `note-map-navigation`                                                                                                                                         |
| Why the view lives in the base and not in the note                    | spec → `note-map-navigation`; rejected copied-base approach → design/history                                                                                         |
| Coordinates from a map link                                           | spec → `location-and-geocoding`                                                                                                                                      |
| Place search                                                          | spec → `location-and-geocoding`                                                                                                                                      |
| Where the 高德 key lives, and why that is the reader's call           | spec → `location-and-geocoding`; SecretStorage wiring → keep-inline                                                                                                  |
| Reverse geocoding                                                     | spec → `location-and-geocoding`                                                                                                                                      |
| Location                                                              | spec → `location-and-geocoding`                                                                                                                                      |
| Why location is not mobile-only                                       | spec → `location-and-geocoding`; platform history → design/history                                                                                                   |
| Inline `![[track.gpx]]`                                               | spec → `inline-track-maps`                                                                                                                                           |
| A file that stops parsing under an open embed                         | spec → `inline-track-maps`; original failure transcript → design/history/test                                                                                        |
| Track statistics                                                      | spec → `inline-track-maps`; numeric thresholds → test/keep-inline                                                                                                    |
| The profile ↔ map hover link                                          | spec → `inline-track-maps`; layer/source ordering → design/keep-inline                                                                                               |
| KML and TCX                                                           | spec → `track-map-rendering`; parser ambiguity → keep-inline/test                                                                                                    |
| Start/end markers, direction and waypoint names                       | spec → `track-map-rendering` and `inline-track-maps`; layer/icon rationale → design/keep-inline                                                                      |
| The arrow was wrong twice, and only a phone said so                   | design/history and test; reload-specific test precondition → obsolete                                                                                                |
| Opening a spot in another map app                                     | spec → `external-map-links` and `coordinate-datum`                                                                                                                   |
| The two lists behind that menu                                        | spec → `external-map-links`; settings-renderer discoveries → design/history/keep-inline                                                                              |
| Non-obvious things to leave alone                                     | capability scenarios distributed across `native-map-integration`, `track-map-rendering`, `coordinate-datum`, and `inline-track-maps`; narrow contracts → keep-inline |
| Testing                                                               | contributor/README → `CONTRIBUTING.md` and `maintainer-workflow`; obsolete reload recovery → obsolete                                                                |
| Linting, and the community scorecard                                  | contributor/README → `CONTRIBUTING.md` and `maintainer-workflow`; one-off findings → design/history                                                                  |
| The settings tab is declared, not drawn                               | spec → `maintainer-workflow`; renderer probes → design/history/keep-inline                                                                                           |
| The base and its view are picked, not typed                           | spec → `maintainer-workflow` and `note-map-navigation`; live transcript → design/history/test                                                                        |
| Translations                                                          | spec → `maintainer-workflow`; contributor instructions → `CONTRIBUTING.md`                                                                                           |
| Releasing                                                             | spec → `maintainer-workflow`; procedure → `CONTRIBUTING.md`                                                                                                          |

All headings fall under the ten capabilities in `design.md`; entry-point commands/layout and the testing/lint/settings/i18n/release headings are the documented `maintainer-workflow` bucket. No eleventh capability is needed.

## Source-comment inventory

Line numbers refer to the pre-cleanup baseline. For every file below, all non-trivial blocks not named under another disposition are classified **keep-inline** because they state adjacent units, malformed-input handling, async ordering, resource ownership, public/undocumented API shape, or a local invariant. Section dividers and terse catch-body notes are mechanical rather than non-trivial and are excluded. Ranges can include nearby executable lines between separately classified blocks; the cleanup pass uses the individual comment boundaries recorded in the source.

| Source file                     | Spec / design / test / contributor disposition                                                                                                                                                                | Obsolete or duplicate disposition                                                                                                                                                                                                            |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `constants.ts`                  | Spec: 4–7, 35–55, 75–85, 118–152 → track/photo/pin specs. Design/test: 10–22 and 91–100 retain only the bounds and executable proof.                                                                          | Delete measured photo transcript and export-history prose; replace the 118–138 `CLAUDE.md` explanation link.                                                                                                                                 |
| `coords.ts`                     | Spec: 1–29, 53, 121–146, 196–208, 239–243 → `coordinate-datum`. Test: 43–50, 88–105 conversion constants/tolerance.                                                                                           | Remove duplicated provider overview once the spec is canonical; keep formulas and units adjacent.                                                                                                                                            |
| `embed.ts`                      | Spec: 1–8, 103–121, 161–182, 198–217, 259–329, 342–390, 429–517, 534–674, 816–859, 867–970 → inline/photo specs. Design/test: 397–400, 698–755, 937–940.                                                      | Replace `CLAUDE.md` references at 80–84, 442, 448, and 873; delete feature-history and measurement narration while retaining failure-before-teardown, layer order, accessibility, and resource-lifecycle contracts.                          |
| `exif.ts`                       | Spec: 1–27 and 668–694 → `photo-map-rendering`. Test/keep-inline: byte bounds, TIFF/ISOBMFF ambiguity, time and orientation rules throughout 57–646.                                                          | Delete the measured Xiaomi claim at 678–681 and shorten the architectural file header.                                                                                                                                                       |
| `geocode.ts`                    | Spec: 1–45, 71–90, 105–109, 157–199, 224–308 → `location-and-geocoding`. Test: provider-error cases at 157–163 and 224–268.                                                                                   | Remove live invalid-key transcripts; keep request/response and datum contracts.                                                                                                                                                              |
| `geolink.ts`                    | Spec: 1–20, 78–85, 92–209, 226–326 → `location-and-geocoding`. Test/keep-inline: provider axis order, terminal CRS, null-number guard.                                                                        | Remove duplicated feature introduction; retain per-provider parser rules beside readers.                                                                                                                                                     |
| `geometry.ts`                   | Spec/design: 151–221 → track/photo role composition. Test/keep-inline: bounds, knob fallback, style gate, endpoint selection.                                                                                 | Remove duplicated cross-file narration only where types and spec already state it.                                                                                                                                                           |
| `i18n.ts`                       | Contributor: 1–7 and 543–575 → `maintainer-workflow`. Spec: setting descriptions at 177–294.                                                                                                                  | Shorten duplicated pins/tracks/photos architecture at 253–294; keep translator context and placeholder meaning.                                                                                                                              |
| `layers.ts`                     | Spec: 63–160, 180–310, 491–640, 759–821, 1005–1055 → native/track/photo/navigation specs. Design/test: 316–481 and 837–977 icon orientation, order, collision, and decode bounds.                             | Delete live-measurement and rejected-alternative narration; keep source/layer removal order, collision filters, units, async cancellation, orientation-before-crop, and viewport/LRU ownership. Replace the `CLAUDE.md` reference near 889.  |
| `link-modal.ts`                 | Spec: 1–21 and 118–145 → `location-and-geocoding`; keep-inline: clipboard refusal and reparse ordering.                                                                                                       | Shorten the feature introduction.                                                                                                                                                                                                            |
| `locate.ts`                     | Spec: 1–23 and 71–135 → `location-and-geocoding`; test/keep-inline: blank-vs-absent, failure breaker, request dedup, WGS-84 and accuracy units.                                                               | Remove duplicated platform history, retaining the adjacent failure policy.                                                                                                                                                                   |
| `main.ts`                       | Spec: 1–22, 199–234, 280–388, 395–844, 865–1093, 1113–1199 → native, track, inline, navigation, location, and photo capabilities. Contributor/design: 1210–1224 settings migration.                           | Delete feature histories and live timing transcripts while keeping runtime shape checks, write/race safety, and narrow async seams. The reload statement at 138–141 is factual registry lifecycle, not the obsolete repeated-reload warning. |
| `map-block.ts`                  | Spec: 1–162 → `note-map-navigation`; test: link direction, filter composition, view collision, and idempotent write cases.                                                                                    | Delete live-verification narration; retain formula direction and write-safety contracts.                                                                                                                                                     |
| `map-events.ts`                 | Design/keep-inline: 3–36 exact `on`/`off` ownership → `native-map-integration`.                                                                                                                               | None.                                                                                                                                                                                                                                        |
| `maplinks.ts`                   | Spec: 1–48, 62–146, 189–266 → `external-map-links`; test/keep-inline: datum, axes, locale ordering, raw substitution, and unsafe schemes.                                                                     | Shorten the inverse-of-geolink narrative; retain security and provider contracts.                                                                                                                                                            |
| `modal.ts`                      | Spec/design: 1–32 → `note-map-navigation`.                                                                                                                                                                    | Delete copied-base feature history; retain relative-link source path.                                                                                                                                                                        |
| `parse.ts`                      | Spec: 1–8, 88, 127–131, 208–212 → `track-map-rendering`; test/keep-inline: coordinate order, namespace/local-name ambiguity, malformed samples, properties/times alignment.                                   | Shorten the library/rejected-alternative header only.                                                                                                                                                                                        |
| `photo-modal.ts`                | Spec/design: 1–22 → `photo-map-rendering` and `note-map-navigation`; keep-inline: 32–60 cache and close-before-open ordering.                                                                                 | Replace the `CLAUDE.md` link and remove live modal measurement.                                                                                                                                                                              |
| `search-modal.ts`               | Spec: 1–26 and 35–87 → `location-and-geocoding`; keep-inline: provider-wide rate limit, monotonic query revision, stale-response gate.                                                                        | Shorten duplicated network-policy introduction.                                                                                                                                                                                              |
| `settings.ts`                   | Spec/contributor: 27–138, 272–438, 490–952 → `maintainer-workflow` plus navigation/location/photo/external-map settings. Design/test: async map-view discovery and settings-renderer constraints at 288–438.  | Remove live-device transcripts and duplicated feature rationale; keep declarative-control ownership, secret migration, placeholder fallback, async null-vs-empty, focus-preserving update, and typed refresh/write seams.                    |
| `spread.ts`                     | Spec: 1–18, 114–128, 191–264 → `pin-spreading`; test: Mercator/formula/ring/scale bounds at 22–102 and 206–264.                                                                                               | Delete live projection/real-vault measurement transcripts; retain formulas, CSS-pixel units, stable identity, leader grouping, and expression-shape contracts.                                                                               |
| `stats.ts`                      | Spec: 1–14, 34–54, 73–88, 108–223, 245–378 → `inline-track-maps`; test: hysteresis, movement threshold, malformed time alignment, formatting, and profile search.                                             | Delete real-walk measurement stories and rejected-algorithm narration; keep units, thresholds, numerical guards, and parser-ambiguity handling.                                                                                              |
| `track-cache.ts`                | Spec: 10–36, 53–225, 242–318 → track/photo refresh and bounded-read contracts. Keep-inline: immutable snapshots, generations, newest-request wins, per-datum freshness, and projection ownership.             | Replace 242–281 measured head-read transcript with a concise range/fallback contract; retain the justified `fetch` warning and byte slicing.                                                                                                 |
| `track-layer.ts`                | Spec: focus/follow/spread/data/interaction/framing blocks at 77–96, 153–220, 598–712, 806–917, 938–1120, 1134–1289. Design/keep-inline: instance wrapper and lifecycle at 102–145, 232–380, 470–589, 728–793. | Remove live timings, measured pixel values, and duplicated capability narration; retain wrapper restoration, event teardown, coordinate direction, signature/upload gate, source/layer ordering, and one-event click guard.                  |
| `types/obsidian-internals.d.ts` | Keep-inline/design: all provenance and optional runtime-shape comments, especially 1–36, 142–175, 204–241, and 270–348 → `native-map-integration`.                                                            | Replace the `CLAUDE.md` focus link around 210–224 with a precise spec link or local statement.                                                                                                                                               |
| `view-options.ts`               | Keep-inline/design: all blocks (6–54) describe local option ranges and insertion anchors → `maintainer-workflow` / `coordinate-datum`.                                                                        | None.                                                                                                                                                                                                                                        |

The only source-level explanation links to delete or replace are the baseline matches in `spread.ts`, `photo-modal.ts`, `types/obsidian-internals.d.ts`, `constants.ts`, `embed.ts`, and `layers.ts`. The obsolete repeated-`plugin:reload` orphan-view warning and full-window recovery procedure exist in `CLAUDE.md`'s Testing section, with one reload-specific arrow-test precondition; no source block depends on them.

## Reconciliation

Completed on 2026-08-15 after the documentation and source-comment pass.

- Every heading in the baseline inventory now has one destination: observable behavior in the ten capability deltas, contributor workflow in `CONTRIBUTING.md`, repository invariants and links in the concise `CLAUDE.md`, or historical migration evidence in this change's `design.md`/inventory.
- Every source-file row above was audited. Remaining comments are adjacent contracts: units and formulas, parser ambiguity and malformed-input handling, undocumented Obsidian provenance, lifecycle/async ownership, privacy and provider ordering, or write/race safety.
- The six source-level `CLAUDE.md` explanation-link locations were removed or replaced with local contracts; `rg 'CLAUDE\.md' src` returns no matches.
- Live-device measurements, issue/debug transcripts, rejected alternatives, and duplicated feature tours were removed from source. Regression-relevant outcomes remain expressed as spec scenarios and tests.
- The obsolete `/mnt/c/...` target, repeated-`plugin:reload` warning, orphaned-layer/view warning, full-window recovery instruction, and reload-specific test preconditions are absent from contributor docs and `src/`. Useful factual hot-reload setup in README/CONTRIBUTING remains.
- No capability was discarded. Claims classified obsolete were recovery folklore superseded by lifecycle fixes, not current product behavior.
