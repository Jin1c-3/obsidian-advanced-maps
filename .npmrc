# Obsidian installs from a release whose tag is the bare version, and
# .github/workflows/release.yml only triggers on one. npm's default would tag
# v1.2.3 and the release would silently never be built.
tag-version-prefix=
