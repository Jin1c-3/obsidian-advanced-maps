# Copy to `.env`. Where `npm run dev` / `npm run deploy` drop the built plugin.
# Point it at the plugin folder inside a real vault; the folder is created if missing.
OBSIDIAN_PLUGIN_DIR=/path/to/vault/.obsidian/plugins/advanced-maps
